generator client {
  provider = "prisma-client"
  output   = "../generated/prisma"
}

datasource db {
  provider = "postgresql"
}

model count {
  id    String @id @db.VarChar(40)
  count Int?   @default(0)
}
